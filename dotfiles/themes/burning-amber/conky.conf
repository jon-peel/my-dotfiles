conky.config = {
    -- Window
    own_window           = true,
    own_window_type      = 'override',
    own_window_hints     = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    own_window_argb_visual = true,
    own_window_argb_value  = 210,
    own_window_colour    = '0D0800',

    -- Position
    alignment = 'top_right',
    gap_x     = 20,
    gap_y     = 48,

    -- Font / text
    use_xft   = true,
    font      = 'IBM Plex Mono:size=9',
    xftalpha  = 1,

    -- Behaviour
    background         = true,
    update_interval    = 86400,
    double_buffer      = true,
    no_buffers         = true,
    draw_borders       = false,
    draw_outline       = false,
    draw_shades        = false,

    -- Colours
    default_color = 'FFB000',
};

conky.text = [[
${color FF6600}── i3 keys ─────────────────────────${color}

${color FFC940}Apps${color}
  Super + Enter        terminal
  Super + D            launcher

${color FFC940}Windows${color}
  Super + Q            close
  Super + F            fullscreen
  Super + H            split side by side
  Super + V            split top / bottom

${color FFC940}Focus${color}
  Super + ←↑↓→         focus window

${color FFC940}Move${color}
  Super + Shift + ←↑↓→  move window

${color FFC940}Layout${color}
  Super + S            stacking
  Super + W            tabbed
  Super + E            split toggle
  Super + Space        toggle focus float
  Super + Shift+Space  toggle floating

${color FFC940}Workspaces${color}
  Super + 1–5          switch workspace
  Super + Shift + 1–5  move to workspace

${color FFC940}Resize  (Super + R)${color}
  ←↑↓→                 resize window
  Enter / Esc          exit resize

${color FFC940}Session${color}
  Super + Shift + R    restart i3
  Super + Shift + E    exit i3
]];
